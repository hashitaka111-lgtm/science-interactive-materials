# 反応系統図 レイアウト方式の検討

対象データ: `data/organic-reaction-map.json`（ノード47件 / 反応58件）
このドキュメントはステップ1の成果物。実装は行っていない。

---

## 0. 先に測ったデータの形

案の比較に入る前に、実際にJSONを読んで測った数字を置く。以下の判断はすべてこの数字に基づく。

| 項目 | 実測値 |
|---|---|
| ノード | 47 |
| 反応 | 58 |
| **描画する線分** | **67本**（`from`×`to` を展開した数。反応58件のうち多入力6件・多出力3件があるため反応数より増える） |
| うち同じノード対を結ぶ重複 | 2本（酢酸エチル→エタノールを r021 と r022 が別々に張る） |
| 双方向ペア（A→B と B→A が両方ある） | 8ペア（酢酸↔酢酸ナトリウム、フェノール↔ナトリウムフェノキシド など） |
| 有向閉路 | 10個 |
| 連結成分 | **2個（42ノード + 5ノード）** |
| family をまたぐ線 | 9本 |
| 日本語名の長さ | 中央値7文字 / 最大25文字（1,2,3,4,5,6-ヘキサクロロシクロヘキサン） |
| 示性式の長さ | 中央値8文字 / 最大16文字（C6H4(OCOCH3)COOH） |

### 先に共有したい2点

**(1) グラフが2つに分かれている。**
トルエン・ベンズアルデヒド・安息香酸・安息香酸ナトリウム・2,4,6-トリニトロトルエンの5ノードが、残り42ノードと1本もつながっていない。データに「ベンゼン→トルエン」の反応が無いためで、これは正しい（高校範囲にベンゼンからトルエンを作る反応が無い）。
どの配置方式を選んでも、この5ノードの島をどこに置くかは自動では決まらない。**置き場所は人間が決める必要がある。**

**(2) 縦軸に「酸化の段階」を取ると矢印が上下に逆走する。**
閉路が10個ある。エタノール⇄エチレン、酢酸⇄酢酸ナトリウム、フェノール⇄ナトリウムフェノキシドなど、中和・加水分解の対が往復になっているため。
「上から下へ酸化が進む」という読み方は、少なくとも中和と加水分解の線では成立しない。

---

## 1. ノードの配置方式

線の交差数は、実際にレイアウトを組んで線分同士の交差を数えた実測値。

### 案A: group ごとに列を作り、酸化の段階を縦軸にした格子配置

**線の交差: 99〜189本（実測）**

| 並べ方 | 交差数 |
|---|---|
| group 列 × JSON順 | 189 |
| group 列 × 列内順序を最適化 | **99** |
| 列の並び順も最適化 | 111 |

67本の線に対して交差99本。1本の線が平均3回ほど他の線と交わる計算で、系統図として読めない。原因は3つある。

**原因1: 列の高さが極端に不揃い。**

| group | ノード数 |
|---|---|
| その他 | **14** |
| 炭化水素 | 9 |
| カルボン酸 | 7 |
| アルコール | 4 |
| アルデヒド / エステル / フェノール類 | 3 |
| アミン | 2 |
| エーテル / ケトン | **1** |

エーテル列とケトン列はノード1個。その一方で「その他」列に14個が積まれる。列幅を揃えると図の大半が空白になる。

**原因2: 「その他」14件の中身が芳香族の主経路そのもの。**
ニトロベンゼン、クロロベンゼン、ベンゼンスルホン酸、ベンゼンスルホン酸ナトリウム、塩化ベンゼンジアゾニウム、アセトアニリド、p-ヒドロキシアゾベンゼン、TNT、クメンヒドロペルオキシド ── 芳香族置換体のほとんどが官能基の分類上「その他」に落ちる。
つまり **ベンゼン→ニトロベンゼン→アニリン→塩化ベンゼンジアゾニウム→アゾ化合物という芳香族の背骨が、1本の列の中で上下に往復する。** これが交差の最大の発生源になっている。

**原因3: 縦軸が定義できない group が6つある。**
酸化の段階として並べられるのは 炭化水素(0)→アルコール(1)→アルデヒド(2)→カルボン酸(3) の系列だけ。エーテル・ケトン・エステル・フェノール類・アミン・その他 には対応する段階が無い。JSONにも段階を表す値は無い。
仮に段階を与えても、group と酸化段階はほぼ同じ情報なので（アルコール列は全部が段階1）、列内で縦に散らす別の軸が結局もう1本必要になる。

- **反応を1件追加したとき**: 該当列の行が1つ増え、その列の順序を組み直す必要がある。列内順序は交差数に直結する（189→99の差はこれだけで生まれている）ので、追加のたびに列全体の並べ直しが要る。自動でやるならバリセンタ法の実装が必要で、その時点で案Cの半分を実装していることになる。
- **幅375px**: 10列必要。1列37pxでは示性式（中央値8文字）も入らない。横スクロールが必須。
- **脂肪族と芳香族**: group が両者に共通なので、1枚に載せると同じ列に脂肪族と芳香族が混ざる。カルボン酸列に酢酸と安息香酸が並ぶ。系統としては別物なので、切り替えないと読めない。

**結論: この数のこの張り方では成立しない。**

---

### 案B: 座標をJSONに手で持たせる完全固定配置

**線の交差: 1〜3本（実測）**

座標を自由に置けるとき、このグラフがどこまで交差を減らせるかを探索した結果、**交差1本**まで到達した。残った1本はサリチル酸ナトリウム→サリチル酸 と アニリン→塩化ベンゼンジアゾニウム の交差で、これも配置次第で消える可能性がある。

67本の線に対して交差1〜3本というのは、実質的に「交差の無い図」と言ってよい。理由はグラフが疎だから。

- 辺65（重複を除く）／ノード47 → 循環数20。木に近い。
- 平面グラフの辺数上限は 3×47−6＝135。実際は65なので、平面に描ける余地が大きい。

さらに固定配置なら、交差を減らす以外のこともできる。

- 脂肪族の背骨（アセチレン→エチレン→エタノール→アセトアルデヒド→酢酸）を一直線に置く
- 芳香族の背骨（ベンゼン→ニトロベンゼン→アニリン→ジアゾニウム塩→アゾ化合物）を一直線に置く
- 8組の双方向ペアを隣接させ、往復の矢印を短い2本の曲線で表現する
- 孤立している**トルエンの島5ノードを、意図的に離れた位置に置いて「つながっていない」ことを見せる**

**トルエンの島を正しく扱えるのは案Bだけ。** 案Cは島を勝手にどこかへ飛ばし、案Aは列に散らばらせて島であることが分からなくなる。

- **反応を1件追加したとき**: 新しいノードの `x` `y` を人が決める。既存ノードは動かさなくてよいことが多いが、追加場所によっては周囲2〜3個をずらす必要がある。**追加のたびに人の手が要るのが唯一かつ最大の欠点。**
- **幅375px**: 座標を持っているので、375px用に別の座標セット（縦長配置）を持たせられる。これは案Cにはできない。
- **脂肪族と芳香族**: 1枚に置いたうえで、絞り込みで切り替えられる。またぐ線が9本しか無いので、1枚に載せても混線しない。

---

### 案C: 力学モデルによる自動配置

**線の交差: 中央値3本、20回中16回が3本（実測）**

交差数だけ見れば案Bとほぼ同じで、案Aより2桁良い。グラフが木に近いため、力学モデルがよく効く。ただし別の問題が3つある。

**問題1: 実行するたびに図の向きが変わる。**
同じデータ・同じコードで6回流し、エタノールから見たベンゼンの方角を測った結果:

| 実行 | ベンゼンの方角 | 距離 |
|---|---|---|
| 1回目 | +107.6度 | 806 |
| 2回目 | −6.1度 | 807 |
| 3回目 | +21.8度 | 832 |
| 4回目 | +70.1度 | 831 |
| 5回目 | −169.0度 | 809 |
| 6回目 | −157.7度 | 804 |

距離はほぼ一定なのに方角がばらばら。教材としては致命的で、

- 生徒が「エタノールは左のほう」という位置の記憶を作れない
- 授業で「右上のベンゼンから」と指せない
- スクリーンショットを撮って解説に貼っても、次に開くと配置が違う

初期座標を固定すれば毎回同じにはなるが、それは案Bの座標を乱数で決めているのと変わらない。

**問題2: 20回中4回は交差が4〜13本に増える。** 悪い側に落ちたときに人が直せない。

**問題3: ラベルが重なる。**
最小交差の配置で、最も近い2ノードの距離は72。日本語名は中央値7文字（約84px）、最大25文字（約300px）。ノード間隔よりラベルのほうが長い。力学モデルはラベルの大きさを考慮しないので、重なりを避けるには結局ラベル回避の力を追加実装することになる。

- **反応を1件追加したとき**: 何もしなくてよい。**この点だけは案Bに明確に勝る。**
- **幅375px**: 収まるかどうかを事前に保証できない。毎回違う外形が出るため、375px用の調整ができない。
- **脂肪族と芳香族**: 引力だけでは分離しない。family ごとに引力を追加する（クラスタ化）実装が別途必要。

---

## 2. 推奨案と理由

### 案B（座標を固定して持たせる）を推奨する。

理由を、案Cとの比較に絞って書く。案Aは交差99本の時点で落選。

| | 案B | 案C |
|---|---|---|
| 交差 | 1〜3 | 3（ただし4回に1回は悪化） |
| 毎回同じ図になるか | なる | **ならない** |
| トルエンの島を意図的に置けるか | **置ける** | 置けない |
| 375pxに収まる保証 | **できる** | できない |
| ラベルの重なり | 座標を決める時に潰せる | 別途実装が要る |
| 反応追加時の手間 | **座標を手で決める** | 不要 |
| 実装量 | 座標表と直線描画のみ | 力学計算＋ラベル回避＋クラスタ化 |

決め手は3つ。

1. **交差数で案Cが勝っていない。** 案Cの利点は「交差が少ないこと」のはずだが、実測では案Bと同等かむしろ不安定。案Cを選ぶ理由が消える。
2. **毎回同じ図になることが、教材では機能の一部。** 系統図は「暗記の足場」として使われる。位置が記憶に残ることに意味がある。
3. **トルエンの島（5ノード）の扱いが、人の判断でしか決まらない。** データに無い「ベンゼン→トルエン」を補うことは禁止されているので、島は島として見せるしかない。その見せ方は自動配置では作れない。

反応追加時の手間は認める。ただし今回のデータは58反応で確定しており、次に足すと分かっているのは油脂のけん化（3ノード・1反応）だけ。それも独立した島なので、既存の座標に影響しない。手間が問題になる規模ではない。

### 脂肪族と芳香族は1枚に載せる（切り替えない）

またぐ線が9本ある。

| 反応 | またぐ内容 |
|---|---|
| r001 | 炭化カルシウム（無機） → アセチレン |
| r004 | アセチレン → ベンゼン |
| r027 | ベンゼン → シクロヘキサン |
| r031 | プロペン → クメン |
| r033 | クメンヒドロペルオキシド → アセトン |
| r042 | メタノール → サリチル酸メチル |
| r043 | 無水酢酸 → アセチルサリチル酸 |
| r048 | 無水酢酸 → アセトアニリド |
| r056 | ベンゼン → ヘキサクロロシクロヘキサン |

このうち r004（アセチレン3分子→ベンゼン）と r033（クメン法でアセトンが出る）は、入試で「脂肪族と芳香族がつながる場所」として問われる。切り替え式にすると、この9本が両方の画面から消える。
**1枚に載せたうえで、group と type の絞り込みで見たい範囲を絞る**のが良い。PC幅では1枚、375pxでは後述の方法で分割する。

---

## 3. その案で必要になるデータの追加（提案のみ・実際には足していない）

座標の置き場所は2通りある。**推奨は方式2。**

### 方式1: JSONに足す（要承認）

`data/organic-reaction-map.json` のトップレベルに `layout` キーを1つ足す。**`nodes` と `reactions` の中身は一切触らない。**

```
{
  "nodes": [...],        ← 変更しない
  "reactions": [...],    ← 変更しない
  "layout": {
    "viewBox": { "w": 1400, "h": 900 },
    "clusters": [
      { "id": "aliphatic-main", "label": "脂肪族の主系列", "x": 40,  "y": 40,  "w": 620, "h": 820 },
      { "id": "aromatic-main",  "label": "芳香族の主系列", "x": 700, "y": 40,  "w": 660, "h": 620 },
      { "id": "toluene-island", "label": "トルエン系（他とつながらない）", "x": 700, "y": 690, "w": 660, "h": 170 }
    ],
    "nodes": {
      "ethanol":  { "x": 300, "y": 380, "cluster": "aliphatic-main" },
      "benzene":  { "x": 900, "y": 200, "cluster": "aromatic-main" },
      "toluene":  { "x": 760, "y": 740, "cluster": "toluene-island" }
    },
    "edges": {
      "r024": { "curve": -28 },
      "r039": { "curve":  28 },
      "r021": { "curve": -18 }
    },
    "narrow": {
      "viewBox": { "w": 375, "h": 2400 },
      "nodes": { "ethanol": { "x": 94, "y": 520 } }
    }
  }
}
```

| キー | 型 | 意味 |
|---|---|---|
| `layout.viewBox` | 数値2つ | SVGの座標系。PC幅用 |
| `layout.clusters[]` | 配列 | 背景に薄く敷く区画。**トルエンの島を明示するために要る** |
| `layout.nodes.<id>.x` `.y` | 数値 | ノード中心の座標（viewBox座標系） |
| `layout.nodes.<id>.cluster` | 文字列 | 所属する区画のid |
| `layout.edges.<反応id>.curve` | 数値 | 曲げ量。**双方向ペア8組で、往復の2本を重ねずに描くために必要**（正負で反対側に膨らむ） |
| `layout.narrow` | オブジェクト | 375px用の別座標セット。`viewBox` と `nodes` だけを上書きし、無い項目はPC用を継承 |

`edges` の `curve` は8ペア16本と、重複する2本（r021/r022の酢酸エチル→エタノール）に対して必要。それ以外の反応は省略してよい（既定0＝直線）。

### 方式2: HTML側に持つ（承認不要・推奨）

上の `layout` オブジェクトを、`html/organic-reaction-map.html` の中に `const LAYOUT = {...}` として直接書く。JSONは化学データだけを持ち、**1バイトも変更しない。**

推奨する理由:

- 「JSONの内容を書き換えない」という今回の制約に完全に沿う
- 座標は表示のための値で、化学データではない。混ぜないほうが後で扱いやすい
- 375px用の座標セットも同じ場所に置けて、教材ファイルの中で完結する
- 将来ほかの教材で同じJSONを使うとき、この教材専用の座標が邪魔にならない

デメリットは、座標を別の教材で再利用したくなったときにコピーが要ること。ただし座標はこの図のためのものなので、再利用する場面は考えにくい。

**ステップ2は方式2で進めたい。** JSONを再利用可能にしたい意図があれば方式1に切り替えるので、その場合は指示してほしい。

---

## 4. 幅375pxでの表示方法

系統図は横に広い。案Bの想定は 1400×900 で、375pxにそのまま入れると3.7分の1。示性式（中央値8文字）が3pxになって読めない。以下の3案を検討した。

### 案ア: 縦長の別座標を持たせて、縦スクロールで読ませる（推奨）

375px幅に4列入る。

| 列数 | 列幅 | 行数 | 図の高さ |
|---|---|---|---|
| 3列 | 125px | 16 | 1024px |
| **4列** | **94px** | **12** | **768px** |
| 5列 | 75px | 10 | 640px |

4列94pxなら示性式（中央値8文字＝約60px、最大16文字＝約110px）がほぼ入る。図の高さ768pxは、スマホの縦スクロール2画面分。系統図の主軸を縦に倒し、上から下へ流す。

- 横スクロールを発生させない（左右のスワイプはブラウザの戻る操作と衝突しやすい）
- 縦スクロールだけで全体を通読できる
- `layout.narrow` に375px専用の座標を持つので、崩れないことを事前に確認できる

**ラベルの問題は、ノードに示性式を出すことで解く。** 日本語名は最大25文字（約300px）で375pxに入らないが、示性式は最大16文字（半角なので約110px）で入る。
日本語名はノードをタップしたときの詳細パネルに出す。これは表示するフィールドを選んでいるだけで、名称の書き換えではない。

### 案イ: 1枚のまま、ピンチズームとパンで見せる

SVGの `viewBox` を指で操作させる。図は1400×900のまま。

- 初期表示は全体をfitさせる（この時点では読めないが、島が2つあることと全体の形は分かる）
- ノードをタップすると、そのノードを中心に自動でズームインする
- 絞り込みを掛けたときは、残ったノードの外接矩形にfitさせる

利点は座標セットが1つで済むこと。欠点は、初期状態で何も読めないこと。教材として最初の1画面が読めないのは弱い。**案アの補助として、案アの上でもピンチズームは効くようにしておくのが良い。**

### 案ウ: 375pxでは図をやめ、選んだノードの周辺1階層だけを描く

全体図を見せず、化合物の縦リストを出す。1つ選ぶと「入ってくる反応」「出ていく反応」だけを小さな図で描く。次数の最大はエタノールの8なので、1画面に必ず収まる。

| ノード | 次数 |
|---|---|
| エタノール | 8 |
| ベンゼン | 7 |
| 酢酸 | 7 |
| ナトリウムフェノキシド | 7 |

利点は375pxで確実に読めること。欠点は**系統図でなくなること。** 「全体のつながりを一望する」という教材の目的が失われる。単独では採らない。

### 採用したい組み合わせ

**案ア（縦長の別座標）を土台にし、案イのピンチズームを重ねる。**

- 幅768px以上: 1400×900 の横長座標。1枚で全体を表示
- 幅768px未満: `layout.narrow` の縦長座標（375×約2400）に切り替え。縦スクロールで通読
- どちらの幅でも、ピンチとダブルタップでズームできる
- 経路をたどるモードで経路が確定したら、経路上のノードの外接矩形に自動でfitさせる（375pxで経路が画面外に出るのを防ぐ）

案ウは採らない。ただし、経路モードや絞り込みの結果を確認する手段として、図とは別に**該当件数のテキスト表示**は置く（「表示中: 47ノード / 58反応」のような行）。これは検証項目の「絞り込みを解除したとき件数が元に戻る」の確認にもそのまま使える。

---

## 5. 確認したいこと

ステップ2に進む前に、次の4点を確認したい。

1. **配置方式は案B（座標を固定）でよいか。**
2. **座標の置き場所は方式2（HTML側に持つ／JSONは無変更）でよいか。** 方式1（JSONに `layout` キーを追加）を希望する場合はその旨を。
3. **脂肪族と芳香族は1枚に載せる方針でよいか。** またぐ9本を見せるためだが、切り替え式を希望するなら変更する。
4. **トルエンの島（5ノード）の見せ方。** 「他とつながらない」と分かる区画に分けて置く想定だが、ラベルの文言に希望があれば。

なお、375pxでノードに示性式を出し、日本語名を詳細パネルに回す件は、名称の書き換えではなく表示フィールドの選択なので、制約には抵触しないと判断している。ここも認識が違えば指摘してほしい。
