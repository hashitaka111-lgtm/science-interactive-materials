# manifest.json 作成レポート

## 0. 追記：溶解度積2ファイルの追加（2026-08-30）

新規追加された html/ksp-basics.html と html/ksp-ph-control.html を確認し、units-draft.md に2行追加、manifest.json に2件追加しました。既存24件の内容（title/heading/summary/topics/interactions/confidence）は変更していませんが、`order` は化学カテゴリ内のファイル名アルファベット順で振り直しているため、以下の7件で数値が変わっています（内容は変更なし）。

| id | 旧 order | 新 order |
|---|---|---|
| chem-organic-separation | 9 | 11 |
| chem-polyprotic-distribution | 10 | 12 |
| chem-reaction-kinetics | 11 | 13 |
| chem-salt-hydrolysis | 12 | 14 |
| chem-strong-weak-acid-base | 13 | 15 |
| chem-thermochemistry | 14 | 16 |
| chem-titration | 15 | 17 |

追加した2件は以下の通り（いずれも confidence: high、HTMLは読み取りのみ）。

| id | order | unit | summary |
|---|---|---|---|
| chem-ksp-basics | 9 | 化学平衡 / 溶解度積と沈殿生成の判定 | 濃度やKspを動かし、イオン積との比較・型別の溶解度換算・共通イオン効果を数値で確かめる |
| chem-ksp-ph-control | 10 | 化学平衡 / pHによる沈殿制御（硫化物の分別沈殿・両性水酸化物） | pHを動かし、硫化物イオン濃度による分別沈殿と両性水酸化物の再溶解を確かめる |

両ファイルとも requestAnimationFrame・WebGL/3D投影・正誤判定つきクイズ機構は確認されなかった（＝スライダーとグラフ連動のみ）。

以下、1〜4節は初回作成時（24件）の記述をそのまま残していますが、件数・orderの記述は現在26件の状態に読み替えてください。

## 1. 処理件数の確認（初回・24件時点の記録）

- html/ 配下の対象ファイル: **24件**（化学15件・物理9件）→ 現在は**26件**（化学17件・物理9件）
- manifest.json の要素数: **24件**（プログラムで検証済み）→ 現在は**26件**（プログラムで再検証済み）
- id の重複: **なし**（24件すべて一意）→ 26件でも重複なしを再確認済み
- 各要素のキー構成（id/file/subject/unit/order/title/heading/summary/topics/interactions/confidence の11キー）: 全件一致、過不足なし
- `file` に記載した全パスについて実ファイルの存在を確認済み
- `topics` の要素数（3〜6個）、`summary` の文字数（60字以内）: 全件で条件を満たすことをスクリプトで検証済み
- HTMLファイルへの書き込みは一切行っていない（読み取りのみ）

### 前提との不一致について（対応済み）

依頼文では「化学14本・物理9本、計23本」とされていましたが、実際に html/ 配下に存在するのは **24ファイル**（酵素反応速度論 enzyme-kinetics.html を含めた化学15本・物理9本）でした。この差分を作業開始前にご確認いただき、「24本全てを対象とする」とのご回答を得たため、24件で manifest.json を作成しています。

## 2. confidence が low の項目

| ファイル | 推測で埋めたキー | 内容 |
|---|---|---|
| html/reaction-kinetics.html | interactions | 「クイズ」を含めた。根拠は「未知の反応X」のグラフから反応次数を見た目で判断させ、「答え合わせ」ボタンを押すと正体（スクロースの加水分解）と真の速度定数を明かす1問だけの自己採点機構。多肢選択で正誤を判定する典型的なクイズ形式ではないため、この分類が適切かは判断が分かれる。 |

上記以外の23件は、実装コード（スライダーのイベントリスナー、requestAnimationFrame ループの有無、3D投影関数の有無、クイズ機構の有無）を直接確認した上で記載しており、confidence: high としています。

## 3. units-final.md に当てはめられなかったファイル

**なし**。24ファイルすべてが units-draft.md（確定版として使用）の「単元名の候補」列にある単元名にマッピングできました。

## 4. 迷った箇所・判断してほしい点

### (a) `order`（学習順の通し番号）の根拠が単元一覧に存在しない
units-draft.md には「学習順」を示す列がなく、ファイルの並びはファイル名のアルファベット順です。今回は化学15本・物理9本それぞれについて、units-draft.md 中の出現順（＝アルファベット順）をそのまま `order` の1〜15、1〜9に採用しました。これは**実際の医学部受験カリキュラムの学習順とは無関係**です。真の学習順（例：熱化学→反応速度→化学平衡→酸塩基→…のような単元の依存関係）がある場合は、その順序をご指定いただければ `order` を差し替えます。

### (b) 「アニメーション」の判定基準
`requestAnimationFrame` の存在だけでなく、それが「継続的に時間発展する物理シミュレーション」を駆動しているか（例: 単振動が再生され続ける、崩壊がランダムに進む）を条件にしました。以下は `requestAnimationFrame` があっても「アニメーション」に含めていません。
- **enzyme-kinetics.html**: ページ読み込み時に主曲線が1回だけ描き込まれる演出アニメーションのみで、以後は継続しない（`prefers-reduced-motion` でも省略される一過性の効果）ため除外。
- **crystal-lattice.html**: `requestAnimationFrame` はドラッグ回転のスムージング用のみで、時間発展するシミュレーションではないため除外（3D自体は該当するので `3D` は付与）。

この線引きが意図と違う場合はご指摘ください。

### (c) `title` の「サイト名などの接尾辞」削除について
24ファイルすべての `<title>` タグを確認しましたが、サイト名やブランド名の接尾辞（例:「｜〇〇」「- サイト名」）は見当たりませんでした。そのため全件、`<title>` の内容をそのまま使用しています。

### (d) h1 が JavaScript で動的生成される2ファイルの `heading`
`acid-base-distribution.html` と `polyprotic-distribution.html` は `<h1 id="title"></h1>` が空で、JSが初期状態（配列の先頭要素）に応じて文言を生成します。今回は**初期表示時に実際にレンダリングされる文字列**（酢酸／リン酸のケース）を `heading` として採用しました。ページを開いた瞬間に見える文言という解釈です。

### (e) `topics` の粒度
「JavaScript内の描画用の数値や色コード」は除外する指示に従い、ページが見出し・注記・数式として明示的に名指ししている法則・式・現象のみを採用しました。ただし各ページは非常に多くのトピックを扱っているため（例: potential-field.html は等電位線・電気力線・マーチングスクエア法・RK4など多数）、3〜6個という上限内でどれを代表として残すかは主観的な取捨選択です。優先度が違う場合はご指摘いただければ差し替えます。

### (f) enzyme-kinetics.html の科目
前回の指示で「生物→化学」に変更済みのものをそのまま踏襲し、`unit` も「代謝 / 酵素反応とミカエリス・メンテン式」としています。医学部受験の科目区分としてこれでよいかは、貴社のカリキュラム区分に依存するため確認事項として記載します。
